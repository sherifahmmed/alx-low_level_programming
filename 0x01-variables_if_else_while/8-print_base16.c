#include <stdio.h>

/**
 * main - Entry point
 * Description: prints all single digit numbers of base 10 starting from 0.
 * Return: 0.
*/

int main(void)
{
	int i, j;

	for (i = 48; i <= 57; i++)
		putchar(i);

	for (j = 97; j <= 102; i++)
		putchar(j);

	putchar('\n');
	return (0);
}
