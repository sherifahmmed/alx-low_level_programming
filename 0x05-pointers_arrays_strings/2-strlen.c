#include "main.h"

/**
 * _strlen - return the length of a string.
 *
 * @n: the string to get the length of
 *
 * Return: the length of @str.
*/

int _strlen(char *s)
{
	int length = 0;

	while (*str++)
		length++;
	return (length);
}
